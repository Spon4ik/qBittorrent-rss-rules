# Phase 32 - Bidirectional Stremio/Jellyfin Watch Progress Sync

## Summary

Add a first bidirectional watch-progress sync between Stremio and Jellyfin. The slice synchronizes exact in-progress playback positions and completed state for matched movies and series episodes, using newest-progress-wins arbitration with a 30-second minimum position delta.

## Scope

- Add Jellyfin HTTP write-back settings: server URL plus API key, with environment override support.
- Keep Jellyfin SQLite reads as the discovery/progress source, but write progress only through Jellyfin's HTTP API.
- Extend Stremio sync with watch-progress extraction and `datastorePut` write-back while preserving existing library item state fields.
- Add shared watch-progress records and newest-progress arbitration in `app.services.watch_state`.
- Add `app.services.watch_progress_sync` as the provider orchestrator and expose `POST /api/settings/sync-watch-progress`.
- Surface a manual `Sync Watch Progress` action from Settings, run watch-progress sync automatically from the existing Jellyfin/Stremio sync executions, and instrument the sync through the shared operation registry.

## Acceptance Criteria

- Stremio newer progress writes to Jellyfin.
- Jellyfin newer progress writes to Stremio.
- Position deltas under 30 seconds are skipped.
- Missing progress on one side does not clear progress on the other.
- Jellyfin write-back never mutates the SQLite DB directly.
- Settings persist the new Jellyfin write-back values without exposing saved API keys.

## Validation Checklist

- [x] Focused watch-progress arbitration, provider, settings, and route tests pass.
- [x] Full `cmd.exe /c scripts\check.bat` passes.
- [x] Shared Docker backend rebuild passes.
- [x] Docker `/health` returns `status=ok`.

## Status

- Status: implemented and locally/Docker validated as release target `v1.2.0` on 2026-05-27.
- Implemented: shared `WatchProgressRecord`/selection helpers, provider progress collection/write hooks, settings and route/UI integration, operation-status instrumentation, and focused regressions.
- Validation evidence: focused tests passed, Ruff/mypy passed, full `cmd.exe /c scripts\check.bat` passed (`439 passed`, `290 warnings`), shared Docker Compose rebuild passed, and Docker `/health` returned `status=ok` / `app_version=1.2.0`.
- Follow-up fix on 2026-05-27: real provider data showed Jellyfin records for `The Boys` at `tt1190634:S05E01`..`S05E05` while Stremio exposed only the series-level latest state at `tt1190634:S04E08`, so exact episode-key matching produced `matched: 0`. The orchestrator now groups episode progress by series IMDb key, compares the latest provider records per series, and Stremio write-back synthesizes `video_id` from a Jellyfin winner such as `tt1190634:S05E05`. Live dry-run diagnostics now report `matched=2` and would update Stremio from Jellyfin for `tt1190634`; full `cmd.exe /c scripts\check.bat` passed with `431 passed` and `290 warnings`.
- Follow-up fix on 2026-05-27: series writes no longer use whole-title Stremio watched flags. For episode records, Stremio write-back clears `flaggedWatched` / `timesWatched`, sets the current episode `video_id`, and updates the `state.watched` episode bitfield using the series video IDs. Stremio read-back now ignores stale whole-series watched flags for episode completion, uses per-episode `timeWatched` rather than series-level `overallTimeWatched`, and checks either the exact watched bitfield entry or the 95% duration threshold. The orchestrator also sends all completed episodes from the winning series group, not only the latest progress record, so watched Jellyfin episodes can become watched Stremio episodes while the in-progress episode keeps its position. A second same-day live fix handles Jellyfin's in-progress `PlayCount=1` / `Played=0` shape by treating only `Played` as completed, and Stremio explicitly clears the watched bit for in-progress episode writes. Validation evidence: focused regressions passed, full `cmd.exe /c scripts\check.bat` passed, Docker Jellyfin read reports `The Boys` S05E05 as `completed=False`, Docker sync is idempotent, and live Stremio bitfield verification shows S05E01-S05E04 true while in-progress S05E05 is false.
- Automatic sync closeout on 2026-05-27: watch-progress sync now runs from existing Jellyfin/Stremio sync executions, so background auto-sync workers and `Save + Sync ... Now` actions no longer depend on the separate manual button. Jellyfin DB reads now fall back to a temporary read-only snapshot copy if the live DB is momentarily unreadable through Docker/host mounts. Validation evidence: focused automatic/snapshot regressions passed, full `cmd.exe /c scripts\check.bat` passed (`439 passed`, `290 warnings`), shared Docker Compose rebuild passed, Docker `/health` returned `status=ok` / `app_version=1.2.0`, and live Docker `execute_jellyfin_sync(..., allow_metadata_requests=False)` ran watch-progress sync automatically with `matched=3`, no watch-progress errors, and no Jellyfin DB readability failure.
- Follow-up fix on 2026-05-28: scheduled Jellyfin auto-sync status now appends detailed top-error text instead of only counts; ambiguous Jellyfin library matches such as duplicate movie rows sharing IMDb `tt0317705` are skipped instead of counted as sync errors; Stremio watch-progress collection now emits zero-progress placeholders for active library movies/series, allowing Jellyfin-only progress such as `Hacks` `tt11815682:S05E09` to write to Stremio; and shared execution `top_errors()` no longer reports successful watch-progress update messages as errors. Validation evidence: focused regressions passed, full `cmd.exe /c scripts\check.bat` passed (`444 passed`, `290 warnings`), shared Docker Compose rebuild passed, Docker `/health` returned `status=ok` / `app_version=1.2.0`, live Docker `execute_jellyfin_sync(..., allow_metadata_requests=False)` returned no top errors, and live Docker manual `sync_watch_progress(...)` was idempotent with `matched=7`, `skipped=7`, and `errors=0`.
- Watched-completion qB cleanup follow-up on 2026-06-02: completed movie rules and conservatively proven finished-series rules now auto-disable through shared watch-state sync; disabled completion rules are excluded from Jackett fetch batches even when disabled rules are included; disabled rules remove their synced or same-name qB RSS rule instead of recreating disabled remote payloads; and post-cleanup qB category maintenance mirrors the desktop "Remove unused categories" command globally by deleting every qB category with zero torrents through the Web API. Finished-series completion now requires the watched episode to be both the latest released episode and the latest known episode, so known planned/future later episodes keep the rule enabled. The cleanup does not delete torrents, files, tags, feeds, or save paths. Validation evidence: focused qB/sync/Jellyfin/Stremio/watch-state/rule-fetch tests passed; Ruff passed for touched backend/test files; full `cmd.exe /c scripts\check.bat` passed (`463 passed`, `292 warnings`); shared Docker Compose rebuild passed; Docker `/health` returned `status=ok` / `app_version=1.2.15` via `curl.exe`.
- Quota-safe finished-series follow-up on 2026-06-02 is tracked as Phase 34 (`docs/plans/phase-34-quota-safe-series-reenablement.md`) and validated as `v1.2.16`: Jellyfin and Stremio now use a shared Cinemeta-first series catalog lookup before OMDb fallback, so still-open or revived series can stay enabled or be re-enabled without spending OMDb quota during background sync. Validation evidence: focused affected tests passed, full `cmd.exe /c scripts\check.bat` passed (`468 passed`, `292 warnings`), desktop build passed, shared Docker rebuild passed, and Docker `/health` returned `app_version=1.2.16`.
- Transient SQLite-lock follow-up on 2026-06-02: the automatic Jellyfin worker now recognizes SQLAlchemy `OperationalError` values containing `database is locked` as retryable app-DB contention and backs off to the next poll without recording a misleading `Automatic Jellyfin sync failed` status. The worker clears its observed Jellyfin DB marker so the next unlocked tick can retry the sync, while non-lock exceptions still record the normal error message. Validation evidence: the new auto-sync regression failed before the fix, then passed; `tests/test_jellyfin_auto_sync.py` passed (`3 passed`); `tests/test_jellyfin.py tests/test_jellyfin_auto_sync.py` passed (`30 passed`); Ruff passed on touched files; full `cmd.exe /c scripts\check.bat` passed (`471 passed`, `292 warnings`); shared Docker Compose rebuild passed; Docker `/health` returned `status=ok` / `app_version=1.2.17` via `curl.exe` after PowerShell `Invoke-WebRequest` hit the known local `NullReferenceException`.
- Live multi-user configuration follow-up on 2026-07-23: the Jellyfin DB contained `Liam` and `Spon4ik`, so leaving `jellyfin_user_name` blank correctly blocked sync as ambiguous. The saved app setting is now `Spon4ik`; the Docker-side read-only connection test selected it successfully, and the forced recovery sync completed with 0 Jellyfin rule errors (`0 updated`, `21 unchanged`, `311 skipped`) plus `10` watch-progress matches / `47` writes. Docker `/health` remained healthy on `v1.2.18`.
- Series-pack eligibility follow-up on 2026-07-31: existing-episode exclusions now match standalone episode releases without swallowing multi-episode ranges. Backend snapshot filtering and browser filtering evaluate explicit pack ranges against synchronized library inventory, keeping a pack when any included episode is new and hiding it only when every listed episode already exists. This rule is independent of `jellyfin_search_existing_unseen`; the setting retains its standalone existing-episode behavior.
- Movie unwatch write-back follow-up on 2026-08-03: live `Star Wars: The Mandalorian and Grogu` (`tt30825738`) state exposed an asymmetric Stremio write contract. Completed movie writes set `flaggedWatched` / `timesWatched`, but an unwatched or in-progress winner only updated position and left stale `flaggedWatched`, `timesWatched`, and `watched` values intact. Stremio then continued reporting the movie completed and rule sync kept it auto-disabled. Movie writes with `completed=False` now clear all three completion markers while preserving position and unrelated state fields. The regression reproduces the live 72-second progress plus stale completion-marker shape. The focused provider/orchestrator/route slice passed; the full gate passed (`490 passed`, `302 warnings`); the desktop build passed; Docker rebuilt healthy on `v1.3.3`; live write/read-back changed Grogu from completed to incomplete without losing its 72-second position; Stremio rule sync re-enabled the local rule and cleared its completion evidence; and live qB read-back confirmed the recreated rule is enabled.
- Docker write-back and title-authority follow-up on 2026-08-03: the remaining `tt6663582` watch-progress error was first routed to `127.0.0.1:8096` inside the app container, then exposed a stale saved key, then returned 400 from the authenticated session-progress endpoint because the sync is not a live playback session, and finally showed that Stremio's IMDb ID cannot be used as Jellyfin's internal item ID. Shared Compose now supplies `host.docker.internal:8096` plus the existing active Jellyfin key; the orchestrator carries the exact matched Jellyfin provider ID into write-back; and the selected user's item data is updated through `UserItems/{itemId}/UserData`. Automatic Stremio sync no longer overwrites saved Content name / Normalized title; those fields remain static after manual edits and refresh together only through an explicit Metadata Lookup action. Focused regressions and Compose validation pass; the full gate passes with Ruff/mypy clean and all 518 tests green; the WinUI build passes with zero warnings/errors; Docker `/health` returns `v1.4.1`; and two live startup sync paths each completed with `12` matches, `41` writes, and zero watch-progress errors while all 306 Stremio titles remained unchanged.
